# Database Configuration (MariaDB)
# IMPORTANT: Change these default passwords before deployment!
MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_PASSWORD=changeme
MYSQL_DATABASE=nextcloud
MYSQL_USER=nextcloud

# PostgreSQL Configuration (if using PostgreSQL instead)
# POSTGRES_PASSWORD=changeme
# POSTGRES_DB=nextcloud
# POSTGRES_USER=nextcloud

# Redis Configuration
# Redis password is optional and not enabled by default for simplicity
# REDIS_PASSWORD=

# Nextcloud Configuration
NEXTCLOUD_PORT=8080
NEXTCLOUD_ADMIN_USER=admin
# IMPORTANT: Change this default password before deployment!
NEXTCLOUD_ADMIN_PASSWORD=changeme
NEXTCLOUD_TRUSTED_DOMAINS=localhost

# Override settings (for reverse proxy)
OVERWRITEPROTOCOL=http
OVERWRITEHOST=localhost:8080
